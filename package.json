{
	"name": "@sigrea/use",
	"version": "0.2.1",
	"description": "Framework-agnostic Sigrea utilities for signals, scopes, and browser APIs.",
	"license": "MIT",
	"type": "module",
	"packageManager": "pnpm@10.0.0",
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/sigrea/use.git"
	},
	"homepage": "https://github.com/sigrea/use#readme",
	"bugs": {
		"url": "https://github.com/sigrea/use/issues"
	},
	"engines": {
		"node": ">=24"
	},
	"sideEffects": false,
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.mjs",
			"require": "./dist/index.cjs"
		}
	},
	"main": "./dist/index.cjs",
	"types": "./dist/index.d.ts",
	"files": ["dist"],
	"keywords": [
		"signals",
		"reactivity",
		"utilities",
		"scope",
		"browser",
		"typescript",
		"dom"
	],
	"scripts": {
		"build": "unbuild",
		"prepack": "unbuild",
		"smoke": "node --input-type=module -e \"const mod = await import('@sigrea/use'); if (typeof mod.createEvents !== 'function') throw new Error('createEvents export is missing');\" && node -e \"const mod = require('@sigrea/use'); if (typeof mod.createEvents !== 'function') throw new Error('createEvents export is missing');\"",
		"changelog": "changelogen",
		"test": "vitest run",
		"test:coverage": "vitest --coverage",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"format": "biome check .",
		"format:fix": "biome check --write .",
		"cicheck": "pnpm -s test && pnpm -s typecheck && pnpm -s build && pnpm -s smoke && pnpm -s format"
	},
	"peerDependencies": {
		"@sigrea/core": "^0.7.0"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@sigrea/core": "^0.7.0",
		"@vitest/coverage-v8": "^3.2.4",
		"changelogen": "^0.6.2",
		"jsdom": "^24.1.3",
		"lefthook": "1.13.6",
		"tsx": "^4.20.5",
		"typescript": "5.9.3",
		"unbuild": "3.6.1",
		"vitest": "^3.2.4"
	},
	"pnpm": {
		"onlyBuiltDependencies": ["lefthook", "@biomejs/biome"]
	}
}
